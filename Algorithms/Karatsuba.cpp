#include <iostream>

using namespace std;

int Karatsuba(int, int, int);

int main()
{
    int a, b, n;
    cout << "Enter two integers with the same digit." << endl;
    cout << "The digit of the integers is: " << endl;
    cin >> n;
    cout << "The first integer is: " << endl;
    cin >> a;
    cout << "The second integer is: " << endl;
    cin >> b;
}

int Karatsuba(int ai, int bi, int ni)
{
    int pro;
    int ain = ai / (10 *);
    if (ni = 1)
    {
        pro = ai * bi;
    }
    else
    {
    }
}